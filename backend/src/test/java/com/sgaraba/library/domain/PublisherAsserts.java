package com.sgaraba.library.domain;

import static org.assertj.core.api.Assertions.assertThat;

public class PublisherAsserts {

    /**
     * Asserts that the entity has all properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertPublisherAllPropertiesEquals(Publisher expected, Publisher actual) {
        assertPublisherAutoGeneratedPropertiesEquals(expected, actual);
        assertPublisherAllUpdatablePropertiesEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all updatable properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertPublisherAllUpdatablePropertiesEquals(Publisher expected, Publisher actual) {
        assertPublisherUpdatableFieldsEquals(expected, actual);
        assertPublisherUpdatableRelationshipsEquals(expected, actual);
    }

    /**
     * Asserts that the entity has all the auto generated properties (fields/relationships) set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertPublisherAutoGeneratedPropertiesEquals(Publisher expected, Publisher actual) {
        assertThat(actual)
            .as("Verify Publisher auto generated properties")
            .satisfies(a -> assertThat(a.getId()).as("check id").isEqualTo(expected.getId()));
    }

    /**
     * Asserts that the entity has all the updatable fields set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertPublisherUpdatableFieldsEquals(Publisher expected, Publisher actual) {
        assertThat(actual)
            .as("Verify Publisher relevant properties")
            .satisfies(a -> assertThat(a.getName()).as("check name").isEqualTo(expected.getName()));
    }

    /**
     * Asserts that the entity has all the updatable relationships set.
     *
     * @param expected the expected entity
     * @param actual the actual entity
     */
    public static void assertPublisherUpdatableRelationshipsEquals(Publisher expected, Publisher actual) {
        // empty method
    }
}
